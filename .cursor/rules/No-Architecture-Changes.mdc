---
alwaysApply: true
---

Do NOT refactor architecture. Specifically:
- Keep existing component structure — do not split or merge files unless asked
- Keep existing React Query patterns (queryKey arrays, staleTime, enabled flags)
- Do not change usePermissions hook or CompanyContext logic
- Do not change Layout.js structure
- Do not change how userRole/extended_role is resolved
- Preserve all existing useEffect dependencies exactly as-is unless fixing a specific bug
